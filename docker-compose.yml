version: "2.2"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kanban.exercise.com
    command: yarn run start
    volumes:
      - ./app:/app
    ports:
      - "9000:9000"
